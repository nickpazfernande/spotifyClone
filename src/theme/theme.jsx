import { createTheme } from '@material-ui/core/styles';
import { orange } from '@material-ui/core/colors';


//dark theme, green spotify color
const theme = createTheme({
    palette: {
        type: 'dark',
        primary: {
            main: '#1DB954',
        },
        secondary: {
            main: '#1DB954',
        },
    },

});


export default theme;